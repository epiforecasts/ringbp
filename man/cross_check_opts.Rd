% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checking.R
\name{cross_check_opts}
\alias{cross_check_opts}
\title{Cross-check \verb{*_opts()} lists to run the \pkg{ringbp} model}
\usage{
cross_check_opts(delays, event_probs)
}
\arguments{
\item{delays}{a \code{list} with class \verb{<ringbp_delay_opts>}: the
delay distribution \code{function}s for the \pkg{ringbp} model, returned
by \code{\link[=delay_opts]{delay_opts()}}. Contains 4 elements: \code{incubation_period},
\code{onset_to_isolation}, \code{latent_period} and \code{onset_to_self_isolation}}

\item{event_probs}{a \code{list} with class \verb{<ringbp_event_prob_opts>}: the
event probabilities for the \pkg{ringbp} model, returned by
\code{\link[=event_prob_opts]{event_prob_opts()}}. Contains 5 elements: \code{asymptomatic},
\code{presymptomatic_transmission}, \code{alpha}, \code{symptomatic_traced} and
\code{symptomatic_self_isolate}}
}
\value{
\code{TRUE} if all the checks pass or an error or warning is thrown if
the simulation options are incompatible.
}
\description{
Cross-check \verb{*_opts()} lists to run the \pkg{ringbp} model
}
\details{
Currently the only cross-checking is between
\code{onset_to_self_isolation} from \code{\link[=delay_opts]{delay_opts()}} and
\code{symptomatic_self_isolate} from \code{\link[=event_prob_opts]{event_prob_opts()}}.

If \code{delays$onset_to_self_isolation} carries a \code{cross_checked} attribute
set to \code{TRUE}, the checks are skipped and \code{TRUE} is returned immediately.
This lets \code{\link[=scenario_sim]{scenario_sim()}} cross-check the options once and then tag
\code{delays} so that the per-replicate \code{\link[=outbreak_model]{outbreak_model()}} calls do not
repeat the check (and re-emit its warning) for every simulation replicate.
The attribute is set on a local copy of \code{delays} (a single
\code{\link[=scenario_sim]{scenario_sim()}} or \code{\link[=outbreak_model]{outbreak_model()}}) call), and does not persist, so
the warning will be repeated in separate \code{\link[=scenario_sim]{scenario_sim()}} or
\code{\link[=outbreak_model]{outbreak_model()}}) calls.
}
\keyword{internal}
